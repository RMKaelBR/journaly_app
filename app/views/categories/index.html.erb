<h1 class="page_title">The Categories Page</h1>

<ul>
  <% @categories.each do |category|%>
    <li>
      <a href="<%= category_path(category) %>">
        <%= category.name %>
      </a>
    </li>
  <% end %>
</ul>

<br/>
<%= button_to link_to "New Category", new_category_path %>
<br/>
<%= link_to "Bahay", bahay_path %>
<br/>
<%= link_to "Home", root_path %>
